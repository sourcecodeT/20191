{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "gcc build active file",
            "type": "shell",
            "command": "cd scanner/src && gcc scanner.o reader.o charcode.o token.o error.o -o scanner"
        }
    ]
}